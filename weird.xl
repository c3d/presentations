// Self-contradicting type
type goedel is matching (G : not goedel)

// Advanced optimizations even for simple stuff
while Condition loop Body is
    if Condition then
        Body
        while Condition loop Body

// Selecting the best compilation strategy
min X, Y is
    minX is min X
    minY is min Y
    if minX < minY then minX else minY
min X is X
