// Create a type representing a network packet
type packet is matching packet
    Size    : natural
    Payload : array[Size] of byte

// This will pass type check
A : buffer is packet(3, array(0, 0, 0))

// This will fail type check (size mismatch)
B: buffer is packet(4, array(0, 0))
